<Page x:Class="WorkOrderManagementSystem.Views.Pages.HistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Repair History">
    <Grid Background="#F5F5F5" Margin="20">
        <DockPanel>
            <!-- Header -->
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,20">
                <TextBlock Text="Repair History" FontSize="28" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock Text="View all completed repair transactions" FontSize="14" Foreground="#999" Margin="0,0,0,20"/>

                <!-- Stats Row -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <!-- Total Completed -->
                    <Border Background="White" CornerRadius="5" Padding="20" Margin="0,0,20,0" Width="300">
                        <StackPanel>
                            <TextBlock Text="Total Completed" FontSize="12" Foreground="#999" Margin="0,0,0,10"/>
                            <TextBlock Name="TotalCompletedBlock" FontSize="32" FontWeight="Bold" Foreground="#333" Margin="0,0,0,5"/>
                            <TextBlock Text="repair jobs" FontSize="12" Foreground="#999"/>
                        </StackPanel>
                    </Border>

                    <!-- Total Revenue -->
                    <Border Background="White" CornerRadius="5" Padding="20" Margin="0,0,20,0" Width="300">
                        <StackPanel>
                            <TextBlock Text="Total Revenue" FontSize="12" Foreground="#999" Margin="0,0,0,10"/>
                            <TextBlock Name="TotalRevenueBlock" FontSize="32" FontWeight="Bold" Foreground="#333" Margin="0,0,0,5"/>
                            <TextBlock Text="from completed jobs" FontSize="12" Foreground="#999"/>
                        </StackPanel>
                    </Border>

                    <!-- Avg Completion Time -->
                    <Border Background="White" CornerRadius="5" Padding="20" Width="300">
                        <StackPanel>
                            <TextBlock Text="Avg. Completion Time" FontSize="12" Foreground="#999" Margin="0,0,0,10"/>
                            <TextBlock Name="AvgCompletionBlock" FontSize="32" FontWeight="Bold" Foreground="#333" Margin="0,0,0,5"/>
                            <TextBlock Text="per repair job" FontSize="12" Foreground="#999"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Search and Filter Row -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <TextBox Name="SearchBox" 
                             Width="400" 
                             Padding="10" 
                             FontSize="12"
                             Foreground="#999"
                             Background="White"
                             TextChanged="SearchBox_TextChanged"
                             Margin="0,0,20,0">
                        <TextBox.Template>
                            <ControlTemplate TargetType="TextBox">
                                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="10">
                                    <ScrollViewer x:Name="PART_ContentHost"/>
                                </Border>
                            </ControlTemplate>
                        </TextBox.Template>
                    </TextBox>

                    <ComboBox Name="TechnicianFilterCombo" 
                              Width="200" 
                              Height="40"
                              Padding="10"
                              Background="White"
                              SelectionChanged="TechnicianFilter_SelectionChanged"
                              Margin="0,0,20,0">
                        <ComboBoxItem>All Technicians</ComboBoxItem>
                    </ComboBox>

                    <ComboBox Name="MonthFilterCombo" 
                              Width="150" 
                              Height="40"
                              Padding="10"
                              Background="White"
                              SelectionChanged="MonthFilter_SelectionChanged">
                        <ComboBoxItem>All Months</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
            </StackPanel>

            <!-- Completed Transactions -->
            <Border DockPanel.Dock="Bottom" Background="White" CornerRadius="5" Padding="20">
                <StackPanel>
                    <TextBlock Text="Completed Transactions" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                    
                    <DataGrid Name="HistoryGrid" 
                              IsReadOnly="True" 
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              BorderThickness="0"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#F0F0F0">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                <Setter Property="Padding" Value="10,10"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="#333"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Order ID" Binding="{Binding WorkOrderNumber}" Width="100"/>
                            <DataGridTextColumn Header="Customer" Binding="{Binding CustomerDisplay}" Width="180"/>
                            <DataGridTextColumn Header="Device" Binding="{Binding DeviceDisplay}" Width="120"/>
                            <DataGridTextColumn Header="Issue" Binding="{Binding Diagnosis}" Width="150"/>
                            <DataGridTextColumn Header="Technician" Binding="{Binding TechnicianName}" Width="120"/>
                            <DataGridTextColumn Header="Completed Date" Binding="{Binding CompletedAtDisplay}" Width="120"/>
                            <DataGridTextColumn Header="Duration" Binding="{Binding DurationDisplay}" Width="80"/>
                            <DataGridTextColumn Header="Total Cost" Binding="{Binding TotalCost}" Width="100"/>
                            <DataGridTemplateColumn Header="Actions" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="?? View" 
                                                Background="Transparent" 
                                                Foreground="#2196F3"
                                                Click="ViewBtn_Click"
                                                Tag="{Binding WorkOrderNumber}"
                                                Cursor="Hand"
                                                Padding="5"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </DockPanel>
    </Grid>
</Page>
